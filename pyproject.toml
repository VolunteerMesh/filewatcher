[project]
name = "filewatcher"
version = "0.1.0"
description = "FileWatcher — file-change monitor with backup and burst detection"
authors = ["Generated <generated@example.com>"]
requires-python = ">=3.8"

[project.dependencies]
watchdog = "^2.1.0"
psutil = "^5.9.0"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
